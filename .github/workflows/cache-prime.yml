name: Cache Prime Workflow

on:
  push:
    branches:
      - main

jobs:
  cache-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: prime-cache-7806095
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: cache-7806095
        id: cache-step

      - name: Echo cache result
        run: |
          echo "Cache hit: ${{ steps.cache-step.outputs.cache-hit }}"